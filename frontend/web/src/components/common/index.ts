/**
 * Common Components
 *
 * Shared UI components used across the application.
 * These components are part of the main bundle since they're used frequently.
 */

export { default as LoadingSpinner } from './LoadingSpinner';
