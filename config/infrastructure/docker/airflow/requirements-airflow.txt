# Apache Airflow Dependencies - ISOLATED ENVIRONMENT
# Last updated: 2026-01-02
#
# IMPORTANT: This file is for Airflow workers/schedulers ONLY.
# Airflow 2.x requires SQLAlchemy <2.0, which conflicts with the main
# application's SQLAlchemy 2.0+ async requirements.
#
# Deploy Airflow in a separate Docker container using this requirements file.
# The main application uses requirements.txt with SQLAlchemy 2.0+.

# ============================================================================
# APACHE AIRFLOW CORE
# ============================================================================
# Using 2.10.4 - latest stable 2.x release with improved provider support
apache-airflow[celery,postgres,redis,statsd]==2.10.4

# ============================================================================
# AIRFLOW PROVIDERS
# ============================================================================
apache-airflow-providers-postgres==5.13.1
apache-airflow-providers-redis==3.8.2
apache-airflow-providers-celery==3.8.4
apache-airflow-providers-http==4.13.2
apache-airflow-providers-docker==3.14.0

# ============================================================================
# DATABASE (SQLAlchemy 1.4.x for Airflow 2.x compatibility)
# ============================================================================
SQLAlchemy>=1.4.0,<2.0.0
psycopg2-binary==2.9.10
alembic==1.14.0

# ============================================================================
# CACHING & TASK QUEUE
# ============================================================================
redis==5.2.1
celery[redis]==5.4.0
kombu==5.4.2

# ============================================================================
# DATA PROCESSING (compatible versions for Airflow)
# ============================================================================
pandas==2.2.3
numpy==1.26.4

# ============================================================================
# FINANCIAL DATA PROVIDERS
# ============================================================================
alpha-vantage==2.3.1
finnhub-python==2.4.21
polygon-api-client==1.16.3
yfinance==0.2.51

# ============================================================================
# HTTP CLIENTS
# ============================================================================
requests==2.32.3
httpx==0.28.1
aiohttp==3.11.11

# ============================================================================
# VALIDATION & SERIALIZATION
# ============================================================================
pydantic==2.10.4
jsonschema==4.23.0
marshmallow==3.23.2

# ============================================================================
# CONFIGURATION
# ============================================================================
python-dotenv==1.0.1
pyyaml==6.0.2

# ============================================================================
# MONITORING
# ============================================================================
prometheus-client==0.21.1
statsd==4.0.1
structlog==24.4.0
sentry-sdk==2.19.2

# ============================================================================
# UTILITIES
# ============================================================================
pytz==2024.2
python-dateutil==2.9.0.post0
click==8.1.8
tabulate==0.9.0
tqdm==4.67.1
backoff==2.2.1
