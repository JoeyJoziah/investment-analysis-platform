# Analysis report — 2026-05-14

Source: `/sc:analyze` run on branch `fix/operational-verification-and-canonical-gaps`.
Tools: static inventory + two code-archaeologist sub-agents (models, utils).

## Tier A — landed this session

| # | Item | Status |
|---|------|--------|
| 1 | TradingAgents archive move + bridge refactor | committed (commit `853101a`) |
| 2 | README router count + endpoint table | corrected (18 routers, `/api/v1/...`) |
| 3 | `.trivyignore` baseline scaffold | added (re-enable hard-fail per file header) |

## Tier B — planned, NOT executed this session

### B1. `backend/models/` consolidation (sub-agent report)

**Declarative base owner:** single — `backend/models/unified_models.py` defines `class Base(DeclarativeBase)`. All other ORM-bearing files import from it. No competing bases.

**Per-file ownership + external import counts:**

| File | Owns | External imports |
|------|------|------------------|
| `unified_models.py` | `Base` + 30 ORM tables + 7 enums + `create/drop_all_tables` | **80+** (canonical) |
| `database.py` | Pure re-export shim + `Prediction` alias + `SessionLocal`/`get_db_session` lazy proxy | **8** (scripts + 2 tests) |
| `tables.py` | Re-export shim + 8 ML-only enums | **1** (`backend/ml/ml_tables.py`) |
| `schemas.py` | ~60 Pydantic DTOs | **5** |
| `ml_models.py` | `PredictionResult`, `StockDataset`, `LSTMModel`, `TransformerModel`, `LightGBMModel`, `ModelManager` — runtime, **not ORM** | **5** |
| `thesis.py` | `InvestmentThesis` ORM | **5** |
| `api_response.py` | `ApiResponse[T]`, `ErrorResponse`, `PaginationMeta`, helpers | **~18** (all routers + middleware) |
| `monitoring_schemas.py` | 8 Pydantic monitoring DTOs | **1** |

**Duplicate model definitions:** none at table level. But **enum duplication** exists between `schemas.py` (str-Enums) and `unified_models.py` (`*Enum` suffix). Also `CostMetrics` exists as ORM in `unified_models` and as Pydantic DTO in `monitoring_schemas` (acceptable layer split, but namespace-confusing).

**Recommended consolidation:**

| File | Action | Rewrites |
|------|--------|----------|
| `unified_models.py` | **KEEP — canonical** | 0 |
| `api_response.py` | **KEEP** | 0 |
| `schemas.py` | **KEEP** (rename to `dtos.py` optional) | 0 |
| `thesis.py` | **MERGE** into `unified_models.py` | 5 |
| `monitoring_schemas.py` | **KEEP** (or merge into `schemas.py`) | 0–1 |
| `ml_models.py` | **MOVE** to `backend/ml/` (runtime, not schema) | 5 |
| `tables.py` | **DELETE** after migrating 8 ML enums into `backend/ml/ml_tables.py` | 1 |
| `database.py` | **DELETE** (pure shim); update 8 callers to `unified_models`; ensure `SessionLocal` proxy lives in `backend/utils/database` | 8 |

**Total import rewrites:** ~19. Open questions: `Fundamental`/`Fundamentals` alias in `__init__.py`; whether `AlternativeData` was ever migrated; tolerance of the `CostMetrics` namespace clash.

### B2. `backend/utils/` dedup (sub-agent report)

**Pairs/triples assessed:**

- `circuit_breaker.py` (10 prod imports, basic `CircuitBreaker` + `APICircuitBreaker` + `CircuitBreakerManager`) vs `advanced_circuit_breaker.py` (1 import — test only — adds `AdaptiveThresholds`, `ProviderReliabilityTracker`, `CascadingFailurePreventor`). Not a strict superset; advanced is effectively unused.
- `cache_manager.py` (4 imports — facade), `bounded_cache.py` (2 — in-process LRU fallback), `cache_warming.py` (1 — proactive warmer). Genuinely distinct roles.
- `database_optimized.py` (1), `db_read_replicas.py` (1), `db_timescale_init.py` (0). Distinct concerns; low utilization across the board.

**Action plan:**

- **Keep** `circuit_breaker.py`. **Delete or fold** select adaptive features from `advanced_circuit_breaker.py`; only 1 test-side rewrite (or just delete the test).
- **Keep** all three cache modules — they do different jobs.
- **Keep** all three DB modules — they do different jobs.

**Dead-code flags:**

- `db_timescale_init.py` — **0 imports outside itself**. Likely a one-shot script; verify before removal.
- `advanced_circuit_breaker.py` — only referenced by `test_resilience_integration.py`. Effectively dead in production.
- `cache_warming.py`, `db_read_replicas.py`, `database_optimized.py` — 1 import site each; audit actual wiring.

## Tier C — false alarm

- Migration `004_add_cache_storage_table.py` exists; the earlier "gap at 004" finding was a glob-truncation artifact. No action.

## Follow-up tickets to file

1. **Wire `monitoring.py` router or delete it.** It's a dangling file in `backend/api/routers/`.
2. **Re-enable security gating.** `.trivyignore` baseline is in place; flip `continue-on-error` off in `.github/workflows/security-scan.yml` once CVEs are triaged.
3. **Models PR.** Execute Tier B1; ~19 import rewrites; gated on integration tests.
4. **Utils dead-code PR.** Execute Tier B2; remove or merge `advanced_circuit_breaker`; verify `db_timescale_init` actually runs in init flow.
5. **Hardcoded Windows path in `cache_aware_agents.py`.** `TRADINGAGENTS_PATH` defaults to `C:\Users\Devin McGrathj\01.project_files\stockanalysistool\TradingAgents` — will not resolve in CI/Linux. Fine because the fallback to the archived copy works, but worth replacing the default with a relative path or `None`.
6. **Unrelated drift on this branch.** Working tree has modified `datasets/**`, `docs/PERFORMANCE_BENCHMARKS.md`, `frontend/web/package-lock.json`, and many untracked artifacts (`test_reports/`, `validation_results.json`, `ml_models/finbert/`, `models/trained/`, `scripts/setup/*-agents/`, `scripts/agentdb.rvf*`). Add the artifacts to `.gitignore` or commit deliberately.