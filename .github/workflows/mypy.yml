# DEPRECATED (Wave 11 / #104): consolidated into type-check.yml
# This workflow is retained as a no-op shell so historical links remain valid.
name: Type Checking (mypy) [DEPRECATED]

on:
  workflow_dispatch:  # manual only — push/PR type checks live in type-check.yml

# F8-14-002: default the workflow token to read-only; jobs that
# write are granted narrow scopes at job level.
permissions:
  contents: read

jobs:
  mypy:
    name: Deprecated — use type-check.yml
    runs-on: ubuntu-latest
    steps:
      - name: Point to consolidated workflow
        run: |
          echo "This workflow was consolidated into .github/workflows/type-check.yml (issue #104)."
          echo "No mypy job runs here anymore."
