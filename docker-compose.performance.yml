
# F8-13-006: limits expressed in the same deploy.resources shape as the
# base/production files — the short-form memory keys conflict fatally with
# deploy.resources.limits.memory when the files merge.
x-performance-limits: &performance-limits
  deploy:
    resources:
      limits:
        memory: 2g
        cpus: "1.0"
        pids: 200

services:
  backend:
    <<: *performance-limits
    environment:
      - NODE_OPTIONS=--max-old-space-size=1536
      - PYTHONOPTIMIZE=2
      - MALLOC_ARENA_MAX=2
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    
  frontend:
    <<: *performance-limits
    environment:
      - NODE_OPTIONS=--max-old-space-size=1024
      - GENERATE_SOURCEMAP=false
    
  postgres:
    <<: *performance-limits
    command: >
      postgres
      -c shared_buffers=256MB
      -c effective_cache_size=1GB
      -c maintenance_work_mem=128MB
      -c checkpoint_completion_target=0.9
      -c wal_buffers=16MB
      -c default_statistics_target=100
      -c random_page_cost=1.1
      -c effective_io_concurrency=200
      -c work_mem=8MB
      -c min_wal_size=1GB
      -c max_wal_size=4GB
      -c max_worker_processes=4
      -c max_parallel_workers_per_gather=2
      -c max_parallel_workers=4
      -c max_parallel_maintenance_workers=2
    
  redis:
    <<: *performance-limits
    command: >
      redis-server
      --maxmemory 512mb
      --maxmemory-policy allkeys-lru
      --save 60 1000
      --tcp-backlog 511
      --tcp-keepalive 300
      --timeout 0
